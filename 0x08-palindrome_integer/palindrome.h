#ifndef PALINDROME_H
#define PALINDROME_H

/* Include libraries */
#include <stdlib.h>
#include <stdio.h>

/* function declaration */
int is_palindrome(unsigned long n);
int intLen(unsigned long n);

#endif /* PALINDROME_H */
