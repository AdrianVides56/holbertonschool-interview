#ifndef HBTN_H
#define HBTN_H

/* Libraries */
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/* Function Prototypes */
void _puts(char *str);
int _is_digit(char *str);
void _error(void);
char *to_string(int num);

#endif /* HBTN_H */
