#ifndef HBTN_H
#define HBTN_H

/* Libraries */
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/* Function Prototypes */
long long int _is_digit(char *str);
void _error(void);
void to_string(long long int num, char *str);
int _putchar(char c);

#endif /* HBTN_H */
